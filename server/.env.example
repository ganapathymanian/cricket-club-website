# Cricket Club Server - Environment Configuration
# Copy this file to .env and update the values
# ⚠️ NEVER commit .env to source control!

# Server Configuration
PORT=3001
NODE_ENV=development

# MSSQL Database Configuration (REQUIRED for index.js mode)
DB_SERVER=localhost
DB_PORT=1433
DB_NAME=CricketClubDB
DB_USER=
DB_PASSWORD=
DB_ENCRYPT=false

# JWT Configuration (REQUIRED - generate a strong random secret)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=

# CORS - Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,capacitor://localhost

# Super Admin Emails - Comma-separated (NO hardcoded emails in code)
SUPER_ADMIN_EMAILS=

# Google OAuth
GOOGLE_CLIENT_ID=

# Email Configuration (optional)
EMAIL_SERVICE_URL=https://api.resend.com/emails
EMAIL_API_KEY=
FROM_EMAIL=noreply@cricketclub.local

# App URL
APP_URL=http://localhost:5173
